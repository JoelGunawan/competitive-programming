#include <bits/stdc++.h>
#define endl "\n"
#pragma GCC optimize("O3")
#pragma GCC target("avx2")
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--)
    {
        int n, m, i, j;
        cin >> n >> m >> i >> j;
        cout << 1 << " " << 1 << " " << n << " " << m << endl;
    }
    return 0;
}