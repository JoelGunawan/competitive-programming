#include <bits/stdc++.h>
#define ll long long
#define lll __int128
#define pb push_back
#define ub upper_bound
#define lb lower_bound
#define mp make_pair
#define fi first
#define se second
#define ins insert
#pragma GCC optimize("Ofast")
#pragma GCC optimize("unroll-loops")
using namespace std;
int main() { 
    int n;
    cin >> n;
    int l = 1, r = n, d = 1, u = n;
    while(l <= r) {
        int mid = (l + r) / 2;
        
    }
    return 0;
}