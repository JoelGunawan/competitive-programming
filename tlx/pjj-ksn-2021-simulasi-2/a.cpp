#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, k, q;
    cin >> n >> k >> q;
    // find the first n / 2 numbers through the min query
    // and find the last n / 2 numbers through the max query
    // if n is even we can divide easily
    // else we must use tricks for the exact midpoint
    // find first n / 2 + 1 numbers;
    vector<int> numbers;
    
    return 0;
}