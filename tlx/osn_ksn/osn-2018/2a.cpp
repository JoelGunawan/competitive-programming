#include <bits/stdc++.h>
#define ll long long
#define endl "\n"
using namespace std;
void query(ll l, ll r)
{
    
}
int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    string subsoal, s; int k, q;
    cin >> s >> k >> q;
    for(int i = 0; i < q; ++i)
    {
        ll l, r;
        cin >> l >> r;
        query(l, r);
    }
    return 0;
}