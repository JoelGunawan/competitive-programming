#include <bits/stdc++.h>
#define ll long long
#define endl "\n"
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    string subsoal, s; int k, q;
    cin >> s >> k >> q;
    for(int i = 0; i < s.size(); ++i)
    {

    }
    for(int i = 0; i < q; ++i)
    {
        ll l, r;
        cin >> l >> r;
        --l, --r;
        
    }
    return 0;
}