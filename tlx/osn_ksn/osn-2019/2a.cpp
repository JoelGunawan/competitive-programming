#include <bits/stdc++.h>
using namespace std;
struct segtree
{
    int a[1 << 19];

};
int main()
{
    int n, m;
    cin >> n >> m;
    pair<int, int> a[n];
    for(int i = 0; i < n; ++i)
        cin >> a[i].first >> a[i].second;
    // sparse segment tree bisa
    // anyways use binser

    return 0;
}