#include <bits/stdc++.h>
using namespace std;
int main() {
  // mengulangi suatu hal
  // print * 10x
  // cout << "**********" << endl;
  // print * 5x
  // ??? 
  // input n
  // print * nx
  // cara kalo gabisa perulangan...
  /*
  if(n == 1) 
    cout << "*" << endl;
  else if(n == 2)
    cout << "**" << endl;
  //  ...
  */
 // i++ -> tambah 1
 // ++i -> tambah 1
 // i-- -> kurang 1
 // --i -> kurang 1
  for(int i = 0; i < 10; i++) {
    if(i == 3) {
      // stop
      break;
    }
    cout << i << " ";
  }
  cout << endl;
  /*
  while(kondisi) {

  }
  */
  /*
  int x = 0;
  while(x < 10) {
    cout << x << endl;
    x += 3;
  }
  */
  // deklarasi -> variabel
  // kondisi -> selama kondisi terpenuhi (true), kode akan dijalankan
  // setelah kode dijalankan, lakukan operasi
  // for(deklarasi; kondisi; operasi) {
    // kode
  //}
}