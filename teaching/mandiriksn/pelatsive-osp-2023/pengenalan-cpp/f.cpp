#include <bits/stdc++.h>
using namespace std;
int main() {
    double n;
    cin >> n;
    // round() -> pembulatan
    // ceil() -> dibulatkan ke atas terus
    // floor() -> dibulatkan ke bawah terus
    cout << floor(n) << " " << ceil(n) << endl;
}