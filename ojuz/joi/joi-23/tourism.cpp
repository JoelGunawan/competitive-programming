#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, m, q;
    cin >> n >> m >> q;
    // mo's algorithm + lca (bisa insert delete)
    // tiap operasi insert/delete -> O(logn)
    // total kompleksitas: O(Nsqrtlog)
    
}