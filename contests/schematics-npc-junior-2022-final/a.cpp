#include <bits/stdc++.h>
using namespace std;
int main() {
    // find vbs from substring reverse
    // if too much open -> close become open
    // else -> open become close
    // iterate from left
    // if open not enough, reduce bracket, at most by remaining '('
    // 
}