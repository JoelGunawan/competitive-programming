#include <vector>
using namespace std;

vector<int> findA(int N, int Q, vector<int> L, vector<int> R, vector<int> k,
                  vector<int> x) {
  vector<int> ret;
  ret.resize(N, -1);
  // process by smallest R
  // there is minimum amount of 1 and minimum amount of 0
  // process yang paling deket endnya
  // jika butuh 1, maka tambahkan 1
  // jika butuh 0, maka tambahkan 0
  return ret;
}